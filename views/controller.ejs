<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Controller Page</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        /* General Body Styling */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fc;
            margin: 0;
            display: flex;
            overflow-x: hidden;
        }

        /* Sidebar Styling */
        .sidebar {
            width: 250px;
            background-color: #34495e;
            color: #ecf0f1;
            position: fixed;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
            padding-top: 20px;
        }

        .sidebar h3 {
            font-size: 1.5em;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 600;
        }

        .sidebar a {
            color: #ecf0f1;
            padding: 15px 20px;
            margin: 5px 0;
            text-decoration: none;
            font-size: 1.1em;
            width: 90%;
            text-align: left;
            border-radius: 5px;
            transition: background-color 0.3s, transform 0.2s;
        }

        .sidebar a:hover {
            background-color: #1abc9c;
            transform: scale(1.05);
        }

        /* Main Content Styling */
        .main-content {
            margin-left: 250px;
            padding: 20px;
            flex-grow: 1;
        }

        h2 {
            font-size: 1.8em;
            color: #2c3e50;
            text-align: center;
            border-bottom: 2px solid #2c3e50;
            padding-bottom: 5px;
            margin-bottom: 20px;
        }

        /* Controller Section Styling */
        .controller-container {
            width: 90%;
            max-width: 800px;
            background: #fff;
            box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            padding: 20px;
            margin: 20px auto;
        }

        button {
            background-color: #1abc9c;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #16a085;
        }

        footer {
            text-align: center;
            margin-top: 20px;
            font-size: 0.9em;
            color: #7f8c8d;
        }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            .sidebar {
                width: 200px;
            }
            .main-content {
                margin-left: 200px;
            }
            h2 {
                font-size: 1.5em;
            }
        }

        @media (max-width: 480px) {
            .sidebar {
                width: 100px;
            }
            .main-content {
                margin-left: 100px;
            }
            h2 {
                font-size: 1.3em;
            }
            .sidebar a {
                font-size: 0.9em;
                padding: 10px;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <h3>Dashboard</h3>
        <a href="/">Dashboard</a>
        <a href="/controller">Controller</a>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <h2>Controller Panel</h2>

        <div class="controller-container">
            <h3>Control Settings</h3>
            <form action="/controller/update" method="POST">
                <div>
                    <label for="pumpStatus">Pump Status:</label>
                    <select id="pumpStatus" name="pumpStatus">
                        <option value="on">On</option>
                        <option value="off">Off</option>
                    </select>
                </div>

                <div>
                    <label for="temperatureSetting">Temperature Setting (°C):</label>
                    <input type="number" id="temperatureSetting" name="temperatureSetting" min="15" max="30">
                </div>

                <div>
                    <label for="phSetting">pH Setting:</label>
                    <input type="number" id="phSetting" name="phSetting" step="0.1" min="5.0" max="7.5">
                </div>

                <button type="submit">Update Settings</button>
            </form>
        </div>

        <footer>Data updates every 5 seconds</footer>
    </div>

    <script>
        // Any JavaScript for interaction can be added here if needed
    </script>
</body>
</html>
